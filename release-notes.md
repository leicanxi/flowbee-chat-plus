# FlowBee 更新说明（可选）
#
# 用途：想亲自写更新内容时，改这个文件；不想管的时候什么都不用做。
#
# 规则：
#   1. 以 # 开头的行都是注释，不会出现在更新说明里；这个文件里没有实际内容时会被自动忽略。
#   2. 发版时会看这个文件"上次发版之后有没有被改过"：
#        · 改过  → 用这里的内容当更新说明
#        · 没改  → 自动用「上次发版到这次」的提交记录生成
#   3. 所以典型用法是：准备发版时把下面分隔线以下的内容改成一段人话，推代码，完事。
#      下次不写就自动回到提交记录，不用刻意清空。
#
# 写人话的建议：说用户能感知的变化（新功能、修了什么毛病），
# 不用写"重构了 xxx 模块"这种用户看不懂的。
# ------------------------------------------------------------------

# 下面开始写这次要告诉用户的内容（这一行本身会被忽略）：
#
# 示例：
# - 新增 xxx 功能
# - 修复 xxx 场景下的崩溃
